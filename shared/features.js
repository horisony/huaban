/**
 * 画完 AI 视觉识别 + TTS 语音朗读
 *
 * 恢复步骤：
 * 1. 改为 true
 * 2. 取消 App.jsx 中所有 `AI_RECOGNIZE_OFF` 注释块
 * 3. 恢复 `import { recognizeDrawing, speak } from './lib/recognize.js'`
 */
export const ENABLE_AI_RECOGNIZE_AND_VOICE = false;
