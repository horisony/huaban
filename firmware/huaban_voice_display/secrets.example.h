#pragma once

// Copy this file to secrets.h. The real secrets.h is ignored by git.
#define HUABAN_WIFI_SSID "your-2.4GHz-wifi"
#define HUABAN_WIFI_PASSWORD "your-wifi-password"
#define HUABAN_DEVICE_URL "https://huaban.vercel.app/api/device-draw"

// Required. Must match HUABAN_DEVICE_TOKEN on the deployed server.
#define HUABAN_DEVICE_TOKEN "replace-with-a-long-random-token"
